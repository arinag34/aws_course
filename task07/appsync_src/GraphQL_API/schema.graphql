schema {
    query: Query
    mutation: Mutation
}

type Query {
    getEvent(id: ID!): Event
}

type Mutation {
    createEvent(userId: Int!, payLoad: AWSJSON!): Event!
}

type Event {
    id: ID!
    userId: Int!
    createdAt: String!
    payLoad: AWSJSON!
}
